# LifeBookMom OS GitHub Start Script
# C:\LifeBookMom-OS 폴더에서 실행하세요.

git init
git add .
git commit -m "Project019 baseline: LifeBookMom OS current state"

Write-Host ""
Write-Host "이제 GitHub에서 새 저장소를 만든 뒤 아래 명령을 실행하세요:"
Write-Host "git remote add origin https://github.com/<계정명>/<저장소명>.git"
Write-Host "git branch -M main"
Write-Host "git push -u origin main"
